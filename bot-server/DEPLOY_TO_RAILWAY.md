# Деплой Telegram бота на Railway.app

Railway - это современная платформа для деплоя приложений с бесплатным тарифом, который **не засыпает** в отличие от Render.

## Почему Railway?

✅ **Не засыпает** - работает 24/7
✅ **500 часов в месяц бесплатно** - достаточно для бота
✅ **Простой деплой** через GitHub
✅ **Автоматические обновления** при пуше в репозиторий
✅ **Удобный интерфейс** и логи

## Шаг 1: Регистрация на Railway

1. Открой https://railway.app
2. Нажми **"Start a New Project"**
3. Войди через **GitHub** (используй аккаунт romanmakarovirk)

## Шаг 2: Деплой бота

### Вариант А: Через интерфейс Railway (рекомендуется)

1. После входа нажми **"New Project"**
2. Выбери **"Deploy from GitHub repo"**
3. Выбери репозиторий **romanmakarovirk/makarovflow**
4. Railway автоматически определит Node.js проект
5. Нажми **"Deploy Now"**

### Вариант Б: Через Railway CLI

```bash
# Установить Railway CLI
npm install -g @railway/cli

# Войти в аккаунт
railway login

# Перейти в папку бота
cd /Users/romanmakarov/mindflow-app/bot-server

# Инициализировать проект
railway init

# Задеплоить
railway up
```

## Шаг 3: Настройка переменных окружения

После деплоя нужно добавить переменные:

1. В Railway Dashboard открой свой проект
2. Перейди в **Variables**
3. Добавь следующие переменные:

```
TELEGRAM_BOT_TOKEN=8353631022:AAHWAts6QguP9--0S4eWxM1rb0Dr40Cmy2Y
WEB_APP_URL=https://makarovflow.vercel.app
NODE_ENV=production
```

4. Нажми **"Save"** - Railway автоматически передеплоит бота

## Шаг 4: Проверка работы

1. В Railway Dashboard найди **"Deployments"**
2. Дождись статуса **"Success"**
3. Открой логи и убедись, что видишь:
   ```
   ✅ Бот запущен!
   📱 Telegram: @makarovflow_bot
   ```
4. Напиши боту `/start` в Telegram

## Шаг 5: Настройка автоматических деплоев

Railway автоматически деплоит при пуше в main ветку GitHub:

```bash
cd /Users/romanmakarov/mindflow-app
git add .
git commit -m "Update bot"
git push origin main
```

Бот автоматически обновится через 1-2 минуты!

## Альтернатива: Исправить Render

Если хочешь остаться на Render, нужно:

1. Зайти на https://dashboard.render.com
2. Найти сервис **mindflow-bot**
3. Если его нет - создать новый Web Service из GitHub репозитория
4. Добавить переменные окружения (те же что выше)
5. В настройках установить:
   - Build Command: `npm install`
   - Start Command: `node index.js`
   - Plan: **Free**

⚠️ **Проблема:** Render free засыпает через 15 минут, поэтому бот будет отвечать с задержкой.

## Альтернатива 2: Fly.io

У тебя уже есть конфигурация для Fly.io (`fly.toml`):

```bash
# Установить Fly CLI
curl -L https://fly.io/install.sh | sh

# Войти
fly auth login

# Задеплоить
cd /Users/romanmakarov/mindflow-app/bot-server
fly launch --config fly.toml

# Установить переменные
fly secrets set TELEGRAM_BOT_TOKEN=8353631022:AAHWAts6QguP9--0S4eWxM1rb0Dr40Cmy2Y
fly secrets set WEB_APP_URL=https://makarovflow.vercel.app

# Задеплоить
fly deploy
```

## Рекомендация

Я рекомендую **Railway**, так как:
- Самый простой в настройке
- Не требует кредитной карты
- Не засыпает
- Работает стабильно 24/7

## Troubleshooting

### Бот не отвечает после деплоя

1. Проверь логи в Railway Dashboard
2. Убедись, что переменные окружения установлены
3. Проверь, что токен бота правильный
4. Перезапусти деплой

### Ошибка "EADDRINUSE" (порт занят)

Railway автоматически назначает порт через переменную `PORT`. Код уже настроен правильно:
```javascript
const PORT = process.env.PORT || 3000;
```

### Бот работает, но Web App не открывается

Проверь, что `WEB_APP_URL` правильный:
- Должен быть: `https://makarovflow.vercel.app`
- Не должен содержать слэш в конце

---

## Быстрый старт (копируй команды по порядку)

```bash
# 1. Установить Railway CLI
npm install -g @railway/cli

# 2. Войти
railway login

# 3. Перейти в папку бота
cd /Users/romanmakarov/mindflow-app/bot-server

# 4. Инициализировать
railway init

# 5. Задеплоить
railway up

# 6. Добавить переменные (выполни в Railway Dashboard или через CLI)
railway variables set TELEGRAM_BOT_TOKEN=8353631022:AAHWAts6QguP9--0S4eWxM1rb0Dr40Cmy2Y
railway variables set WEB_APP_URL=https://makarovflow.vercel.app
railway variables set NODE_ENV=production
```

Готово! Бот будет работать 24/7 🚀
