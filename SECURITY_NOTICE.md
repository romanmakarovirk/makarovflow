# 🔒 Важное уведомление о безопасности

## ⚠️ КРИТИЧНО: API ключ был раскрыт

**Дата обнаружения:** 2025-12-02

### Проблема

Файл `.env` с реальным API ключом OpenRouter был добавлен в Git репозиторий и виден в истории коммитов.

### Что нужно сделать НЕМЕДЛЕННО

1. **Отозвать скомпрометированный API ключ:**
   - Перейдите на https://openrouter.ai/keys
   - Найдите ключ, начинающийся с `sk-or-v1-42a38200a296...`
   - Нажмите "Revoke" или "Delete"

2. **Создать новый API ключ:**
   - На той же странице создайте новый ключ
   - Скопируйте новый ключ

3. **Обновить локальный .env файл:**
   ```bash
   # Откройте .env файл и замените ключ
   VITE_OPENROUTER_API_KEY=ваш_новый_ключ_здесь
   ```

4. **Убедиться, что .env не в Git:**
   ```bash
   # Проверить статус
   git status

   # Файл .env НЕ должен быть в списке изменений
   # Если он там есть:
   git rm --cached .env
   git commit -m "Remove .env from tracking"
   ```

5. **(Опционально) Очистить историю Git:**
   ```bash
   # ВНИМАНИЕ: это перепишет историю репозитория
   git filter-branch --force --index-filter \
   "git rm --cached --ignore-unmatch .env" \
   --prune-empty --tag-name-filter cat -- --all

   # Затем force push (если репозиторий публичный)
   git push origin --force --all
   ```

### Защита на будущее

1. **Всегда проверяйте .gitignore:**
   - Убедитесь, что `.env` в списке
   - Используйте `.env.example` для шаблонов

2. **Проверка перед коммитом:**
   ```bash
   # Перед каждым коммитом
   git status
   # Убедитесь, что чувствительные файлы не в списке
   ```

3. **Используйте git-secrets:**
   ```bash
   # Установите git-secrets для автоматической проверки
   brew install git-secrets  # macOS
   git secrets --install
   git secrets --register-aws
   ```

### Последствия утечки

- ✅ **Минимальные** - ключ быстро обнаружен
- ⚠️ **Возможны** - несанкционированное использование API (дополнительные расходы)
- 🔒 **Предотвращено** - после отзыва ключа никто не сможет им воспользоваться

### Чек-лист безопасности

- [ ] API ключ отозван
- [ ] Новый API ключ создан
- [ ] .env обновлен с новым ключом
- [ ] .env удален из Git tracking
- [ ] .gitignore содержит `.env`
- [ ] Проверено, что .env не в `git status`
- [ ] (Опционально) История Git очищена

---

**Дата завершения:** _____________

**Подтверждение:** Я выполнил все шаги чек-листа
