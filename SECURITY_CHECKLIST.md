# ✅ Чеклист безопасности - Что нужно сделать НЕМЕДЛЕННО

## 🔴 КРИТИЧНО - Сделать ПРЯМО СЕЙЧАС:

### 1. Ротация всех токенов и ключей
- [ ] **Сгенерировать новый Telegram Bot Token** (через @BotFather)
- [ ] **Создать новый OpenRouter API ключ**
- [ ] **Изменить пароль базы данных** (если используется PostgreSQL)
- [ ] **Сгенерировать новый JWT_SECRET** (минимум 32 символа)

### 2. Настроить GitHub Secrets
- [ ] Перейти в GitHub → Settings → Secrets and variables → Actions
- [ ] Добавить `VITE_OPENROUTER_API_KEY` в Secrets
- [ ] Проверить что workflow использует `${{ secrets.VITE_OPENROUTER_API_KEY }}`

### 3. Настроить Environment Variables на хостинге

**Для Render.com (bot-server):**
- [ ] Добавить `TELEGRAM_BOT_TOKEN` в Environment Variables
- [ ] Добавить `WEB_APP_URL` в Environment Variables
- [ ] Добавить `RENDER_EXTERNAL_URL` в Environment Variables

**Для Netlify/Vercel (frontend):**
- [ ] Добавить `VITE_OPENROUTER_API_KEY` в Environment Variables

**Для API Server (если используется):**
- [ ] Добавить `DATABASE_URL` в Environment Variables
- [ ] Добавить `JWT_SECRET` в Environment Variables
- [ ] Добавить `ALLOWED_ORIGINS` в Environment Variables (опционально)

### 4. Очистить историю Git от секретов

⚠️ **ВНИМАНИЕ:** Это важно, но требует осторожности!

Вариант 1 (рекомендуется): Использовать BFG Repo-Cleaner
```bash
# Скачать BFG: https://rtyley.github.io/bfg-repo-cleaner/
java -jar bfg.jar --replace-text passwords.txt

# В passwords.txt добавить строки с токенами:
# старый_токен==>НЕМЕДЛЕНО_ЗАМЕНИТЬ_В_GIT
```

Вариант 2: Использовать git filter-branch
```bash
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch bot-server/index.js database/setup.js" \
  --prune-empty --tag-name-filter cat -- --all
```

**ВАЖНО:** После очистки:
- Force push: `git push origin --force --all`
- Уведомить всех разработчиков о необходимости переклонировать репозиторий

### 5. Проверить что .env файлы в .gitignore
- [x] ✅ Уже добавлено в .gitignore

### 6. Создать .env.example файлы

Создать примеры файлов с пустыми значениями:
- `bot-server/.env.example`
- `api-server/.env.example`
- `.env.example` (для frontend)

---

## 📋 Проверочный список перед деплоем:

- [ ] Все токены удалены из кода
- [ ] Все токены добавлены в Environment Variables
- [ ] GitHub Secrets настроены
- [ ] `.env` файлы добавлены в `.gitignore`
- [ ] Локальные `.env` файлы созданы (не коммитить!)
- [ ] Тесты проходят (если есть)
- [ ] Приложение запускается локально с переменными окружения

---

## 🔐 Дополнительные меры безопасности:

### Настроить позже (не критично, но рекомендуется):

1. **Двухфакторная аутентификация** на всех сервисах
2. **Логирование доступа** к API
3. **Мониторинг подозрительной активности**
4. **Регулярная ротация токенов** (раз в 3-6 месяцев)
5. **Backup стратегия** для данных
6. **SSL/TLS сертификаты** (уже есть на Vercel/Netlify)

---

## ⚠️ Если токены уже скомпрометированы:

1. **НЕМЕДЛЕННО** сгенерировать новые токены
2. **НЕМЕДЛЕННО** обновить их на всех сервисах
3. Проверить логи на подозрительную активность
4. При необходимости заблокировать старые токены

---

**Дата создания:** 2025-01-27  
**Статус:** ✅ Код исправлен, требуется настройка окружения

